// Compare vectors 
#include <stdio.h>
#include "bncmatmul.h"

// a[i] == b[i] for 0 < i < dim
// return dim -> all elements are true
// return 0 to dim - 1 -> some elements are false
long int compare_dvector_eq(long int *index, DVector a, DVector b)
{
    
}